<div class="max-w-3xl mx-auto px-4 py-8">
  <h1 class="text-2xl font-bold mb-6">記事を編集</h1>
  <%= render "form", article: @article %>
  <div class="mt-4">
    <%= link_to "← 記事に戻る", article_path(@article), class: "text-blue-600 hover:underline" %>
  </div>
</div>
