# Rails Blog Phase 1 (ERB) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rails + ERBで動くブログを作る（記事CRUD + Devise認証）

**Architecture:** フルスタックRails（API-onlyではない）。MVCアーキテクチャでコントローラー・モデル・ERBビューを実装。認証はDeviseを使用。

**Tech Stack:** Ruby on Rails 8.x, SQLite3, Devise, Tailwind CSS, ERB

---

## ファイル構成

```
rails-blog/
  Gemfile                                 # devise追加
  config/routes.rb                        # devise_for + resources :articles + root
  db/migrate/
    XXXXXX_devise_create_users.rb         # Deviseが自動生成
    XXXXXX_create_articles.rb             # タイトル・本文・公開日・user_id
  app/
    models/
      user.rb                             # Deviseモジュール + has_many :articles
      article.rb                          # belongs_to :user, バリデーション
    controllers/
      application_controller.rb          # current_user, authenticate系ヘルパー
      articles_controller.rb             # CRUD 7アクション + 認可
    views/
      layouts/
        application.html.erb             # ナビゲーション（ログイン状態で切り替え）
      articles/
        index.html.erb                   # 記事一覧
        show.html.erb                    # 記事詳細
        new.html.erb                     # 新規作成フォーム
        edit.html.erb                    # 編集フォーム
        _form.html.erb                   # new/editで共有するフォームパーシャル
```

---

## Task 1: Railsアプリのセットアップ

**Files:**
- Create: `rails-blog/` 配下すべて（rails newで生成）

- [ ] **Step 1: Railsアプリを生成する**

既存の`rails-blog`ディレクトリ内で実行。`--skip-git`は既にgitリポジトリがあるため。

```bash
cd /Users/katanabe/ghq/github.com/katanabe/rails-blog
rails new . --css=tailwind --skip-git
```

既存ファイルの上書き確認が出た場合は`Y`（yes）で進む。

- [ ] **Step 2: サーバーが起動することを確認**

```bash
bin/dev
```

ブラウザで `http://localhost:3000` にアクセスして「Yay! You're on Rails!」が表示されればOK。`Ctrl+C`で停止。

- [ ] **Step 3: コミット**

```bash
git add .
git commit -m "chore: rails new with tailwind"
git push
```

---

## Task 2: Deviseのセットアップ（ユーザー認証）

**Files:**
- Modify: `Gemfile`
- Create: `config/initializers/devise.rb`（devise:installで生成）
- Create: `db/migrate/XXXXXX_devise_create_users.rb`（devise Userで生成）
- Modify: `app/models/user.rb`
- Modify: `config/routes.rb`

- [ ] **Step 1: GemfileにDeviseを追加**

`Gemfile`の末尾に追加：

```ruby
gem "devise"
```

- [ ] **Step 2: bundle install**

```bash
bundle install
```

- [ ] **Step 3: Deviseのインストール**

```bash
bin/rails generate devise:install
```

ターミナルにいくつか指示が出る。次のステップで対応する。

- [ ] **Step 4: deviseの設定（メーラーのデフォルトURL）**

`config/environments/development.rb` に以下を追加（`Rails.application.configure do` ブロック内）：

```ruby
config.action_mailer.default_url_options = { host: 'localhost', port: 3000 }
```

- [ ] **Step 5: Userモデルを生成**

```bash
bin/rails generate devise User
```

`db/migrate/XXXXXX_devise_create_users.rb` と `app/models/user.rb` が生成される。

- [ ] **Step 6: マイグレーション実行**

```bash
bin/rails db:migrate
```

期待される出力（例）:
```
== CreateUsers: migrating ====================================
-- create_table(:users)
== CreateUsers: migrated =====================================
```

- [ ] **Step 7: Deviseのビューを生成（ログインフォームをカスタマイズできるように）**

```bash
bin/rails generate devise:views
```

- [ ] **Step 8: サインアップ・ログインが動くことを確認**

```bash
bin/dev
```

`http://localhost:3000/users/sign_up` にアクセスしてサインアップフォームが表示されればOK。`Ctrl+C`で停止。

- [ ] **Step 9: コミット**

```bash
git add .
git commit -m "feat: add devise authentication"
git push
```

---

## Task 3: Articleモデルの作成

**Files:**
- Create: `db/migrate/XXXXXX_create_articles.rb`
- Create: `app/models/article.rb`
- Modify: `app/models/user.rb`

- [ ] **Step 1: Articleモデルを生成**

```bash
bin/rails generate model Article title:string body:text published_at:datetime user:references
```

`user:references` でuser_idカラムと外部キー制約が自動で追加される。

- [ ] **Step 2: マイグレーション実行**

```bash
bin/rails db:migrate
```

- [ ] **Step 3: Articleモデルにバリデーションを追加**

`app/models/article.rb` を以下にする：

```ruby
class Article < ApplicationRecord
  belongs_to :user
  validates :title, presence: true
  validates :body, presence: true

  scope :published, -> { where.not(published_at: nil).order(published_at: :desc) }
end
```

- [ ] **Step 4: UserモデルにArticleとの関連を追加**

`app/models/user.rb` の`class User < ApplicationRecord`ブロック内、`devise`行の下に追加：

```ruby
has_many :articles, dependent: :destroy
```

- [ ] **Step 5: Railsコンソールで動作確認**

```bash
bin/rails console
```

コンソール内で：

```ruby
# ユーザー作成
user = User.create!(email: "test@example.com", password: "password123", password_confirmation: "password123")

# 記事作成
article = user.articles.create!(title: "テスト記事", body: "本文です", published_at: Time.current)

# 取得確認
Article.published.first.title  # => "テスト記事"
exit
```

- [ ] **Step 6: コミット**

```bash
git add .
git commit -m "feat: add Article model with user association"
git push
```

---

## Task 4: ルーティングとコントローラーの作成

**Files:**
- Modify: `config/routes.rb`
- Create: `app/controllers/articles_controller.rb`

- [ ] **Step 1: ルーティングを設定**

`config/routes.rb` を以下にする：

```ruby
Rails.application.routes.draw do
  devise_for :users
  resources :articles
  root to: "articles#index"
end
```

- [ ] **Step 2: ルーティングを確認**

```bash
bin/rails routes | grep articles
```

期待される出力（抜粋）：
```
    articles GET    /articles(.:format)          articles#index
             POST   /articles(.:format)          articles#create
 new_article GET    /articles/new(.:format)      articles#new
edit_article GET    /articles/:id/edit(.:format) articles#edit
     article GET    /articles/:id(.:format)      articles#show
             PATCH  /articles/:id(.:format)      articles#update
             DELETE /articles/:id(.:format)      articles#destroy
```

- [ ] **Step 3: Articlesコントローラーを作成**

`app/controllers/articles_controller.rb` を作成：

```ruby
class ArticlesController < ApplicationController
  before_action :authenticate_user!, only: [:new, :create, :edit, :update, :destroy]
  before_action :set_article, only: [:show, :edit, :update, :destroy]
  before_action :authorize_user!, only: [:edit, :update, :destroy]

  def index
    @articles = Article.published
  end

  def show
  end

  def new
    @article = current_user.articles.build
  end

  def create
    @article = current_user.articles.build(article_params)
    if @article.save
      redirect_to @article, notice: "記事を投稿しました"
    else
      render :new, status: :unprocessable_entity
    end
  end

  def edit
  end

  def update
    if @article.update(article_params)
      redirect_to @article, notice: "記事を更新しました"
    else
      render :edit, status: :unprocessable_entity
    end
  end

  def destroy
    @article.destroy
    redirect_to articles_path, notice: "記事を削除しました"
  end

  private

  def set_article
    @article = Article.find(params[:id])
  end

  def authorize_user!
    redirect_to root_path, alert: "権限がありません" unless @article.user == current_user
  end

  def article_params
    params.require(:article).permit(:title, :body, :published_at)
  end
end
```

- [ ] **Step 4: コミット**

```bash
git add .
git commit -m "feat: add articles controller and routes"
git push
```

---

## Task 5: ERBビューの作成

**Files:**
- Create: `app/views/articles/index.html.erb`
- Create: `app/views/articles/show.html.erb`
- Create: `app/views/articles/new.html.erb`
- Create: `app/views/articles/edit.html.erb`
- Create: `app/views/articles/_form.html.erb`

- [ ] **Step 1: 記事一覧ビュー（index）を作成**

`app/views/articles/index.html.erb`：

```erb
<div class="max-w-3xl mx-auto px-4 py-8">
  <div class="flex justify-between items-center mb-6">
    <h1 class="text-3xl font-bold">ブログ記事一覧</h1>
    <% if user_signed_in? %>
      <%= link_to "新しい記事を書く", new_article_path, class: "bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700" %>
    <% end %>
  </div>

  <% if @articles.empty? %>
    <p class="text-gray-500">まだ記事がありません。</p>
  <% else %>
    <% @articles.each do |article| %>
      <div class="border rounded-lg p-6 mb-4 shadow-sm">
        <h2 class="text-xl font-semibold mb-2">
          <%= link_to article.title, article_path(article), class: "hover:underline" %>
        </h2>
        <p class="text-gray-500 text-sm">
          <%= article.published_at.strftime("%Y年%m月%d日") %>
          by <%= article.user.email %>
        </p>
        <p class="mt-2 text-gray-700"><%= truncate(article.body, length: 100) %></p>
      </div>
    <% end %>
  <% end %>
</div>
```

- [ ] **Step 2: 記事詳細ビュー（show）を作成**

`app/views/articles/show.html.erb`：

```erb
<div class="max-w-3xl mx-auto px-4 py-8">
  <% if notice %>
    <div class="bg-green-100 text-green-800 px-4 py-2 rounded mb-4"><%= notice %></div>
  <% end %>

  <h1 class="text-3xl font-bold mb-2"><%= @article.title %></h1>
  <p class="text-gray-500 text-sm mb-6">
    <%= @article.published_at&.strftime("%Y年%m月%d日") %>
    by <%= @article.user.email %>
  </p>

  <div class="prose max-w-none mb-8">
    <%= simple_format @article.body %>
  </div>

  <% if user_signed_in? && @article.user == current_user %>
    <div class="flex gap-4">
      <%= link_to "編集", edit_article_path(@article), class: "bg-yellow-500 text-white px-4 py-2 rounded hover:bg-yellow-600" %>
      <%= button_to "削除", article_path(@article), method: :delete,
          data: { turbo_confirm: "本当に削除しますか？" },
          class: "bg-red-600 text-white px-4 py-2 rounded hover:bg-red-700" %>
    </div>
  <% end %>

  <div class="mt-6">
    <%= link_to "← 一覧に戻る", articles_path, class: "text-blue-600 hover:underline" %>
  </div>
</div>
```

- [ ] **Step 3: フォームパーシャル（_form）を作成**

`app/views/articles/_form.html.erb`：

```erb
<%= form_with model: article, class: "space-y-4" do |f| %>
  <% if article.errors.any? %>
    <div class="bg-red-100 text-red-700 px-4 py-2 rounded">
      <ul>
        <% article.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.label :title, "タイトル", class: "block font-medium mb-1" %>
    <%= f.text_field :title, class: "w-full border rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-400" %>
  </div>

  <div>
    <%= f.label :body, "本文", class: "block font-medium mb-1" %>
    <%= f.text_area :body, rows: 10, class: "w-full border rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-400" %>
  </div>

  <div>
    <%= f.label :published_at, "公開日時（空欄で下書き）", class: "block font-medium mb-1" %>
    <%= f.datetime_local_field :published_at, class: "border rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-blue-400" %>
  </div>

  <div>
    <%= f.submit class: "bg-blue-600 text-white px-6 py-2 rounded hover:bg-blue-700 cursor-pointer" %>
  </div>
<% end %>
```

- [ ] **Step 4: 新規作成ビュー（new）を作成**

`app/views/articles/new.html.erb`：

```erb
<div class="max-w-3xl mx-auto px-4 py-8">
  <h1 class="text-2xl font-bold mb-6">新しい記事を書く</h1>
  <%= render "form", article: @article %>
  <div class="mt-4">
    <%= link_to "← 一覧に戻る", articles_path, class: "text-blue-600 hover:underline" %>
  </div>
</div>
```

- [ ] **Step 5: 編集ビュー（edit）を作成**

`app/views/articles/edit.html.erb`：

```erb
<div class="max-w-3xl mx-auto px-4 py-8">
  <h1 class="text-2xl font-bold mb-6">記事を編集</h1>
  <%= render "form", article: @article %>
  <div class="mt-4">
    <%= link_to "← 記事に戻る", article_path(@article), class: "text-blue-600 hover:underline" %>
  </div>
</div>
```

- [ ] **Step 6: ブラウザで全ページを確認**

```bash
bin/dev
```

以下を確認：
- `http://localhost:3000/` → 記事一覧
- `http://localhost:3000/users/sign_up` → サインアップ
- サインアップ後 → `http://localhost:3000/articles/new` で記事作成
- 作成した記事が一覧に表示される
- 詳細ページから編集・削除できる
- ログアウト後は new/edit/delete リンクが非表示になる

- [ ] **Step 7: コミット**

```bash
git add .
git commit -m "feat: add ERB views for articles"
git push
```

---

## Task 6: ナビゲーションの追加

**Files:**
- Modify: `app/views/layouts/application.html.erb`

- [ ] **Step 1: レイアウトにナビゲーションを追加**

`app/views/layouts/application.html.erb` の `<body>` タグの直後、`<%= yield %>` の前に追加：

```erb
<nav class="bg-white border-b shadow-sm">
  <div class="max-w-3xl mx-auto px-4 py-3 flex justify-between items-center">
    <%= link_to "Rails Blog", root_path, class: "text-xl font-bold text-blue-600" %>

    <div class="flex items-center gap-4">
      <% if user_signed_in? %>
        <span class="text-gray-600 text-sm"><%= current_user.email %></span>
        <%= link_to "新規投稿", new_article_path, class: "text-blue-600 hover:underline text-sm" %>
        <%= button_to "ログアウト", destroy_user_session_path, method: :delete,
            class: "text-sm text-gray-600 hover:underline bg-transparent border-none cursor-pointer" %>
      <% else %>
        <%= link_to "ログイン", new_user_session_path, class: "text-blue-600 hover:underline text-sm" %>
        <%= link_to "サインアップ", new_user_registration_path, class: "bg-blue-600 text-white px-3 py-1 rounded text-sm hover:bg-blue-700" %>
      <% end %>
    </div>
  </div>
</nav>

<% if alert %>
  <div class="bg-red-100 text-red-800 px-4 py-2 text-center"><%= alert %></div>
<% end %>
```

- [ ] **Step 2: 全体動作を最終確認**

```bash
bin/dev
```

以下のフローを一通り確認：
1. サインアップ → ログイン状態でナビに表示
2. 記事作成（公開日時あり）→ 一覧に表示
3. 記事作成（公開日時なし）→ 一覧に**表示されない**（下書き）
4. 他ユーザーの記事を編集しようとする → root_pathにリダイレクト
5. ログアウト → ログイン・サインアップボタンが表示

- [ ] **Step 3: コミット**

```bash
git add .
git commit -m "feat: add navigation layout"
git push
```

---

## Phase 1 完了 🎉

これでフェーズ1（Rails + ERB）が完成です。

**学んだこと:**
- `rails new` でのアプリ生成
- MVCアーキテクチャ（Model / Controller / View の役割）
- `rails generate model` でのマイグレーション
- ActiveRecordのバリデーション・アソシエーション（`has_many` / `belongs_to`）
- RESTfulルーティング（`resources`）
- Deviseによる認証（`authenticate_user!`・`current_user`・`user_signed_in?`）
- `before_action` によるアクセス制御
- ERBテンプレートとパーシャル（`render "form"`）

**次のステップ（Phase 2）:** Inertia.js + Reactへの移行
