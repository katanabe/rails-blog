<% password_hash = Devise::Encryptor.digest(User, "password") %>

one:
  email: one@example.com
  encrypted_password: <%= password_hash %>
  jti: <%= SecureRandom.uuid %>

two:
  email: two@example.com
  encrypted_password: <%= password_hash %>
  jti: <%= SecureRandom.uuid %>
